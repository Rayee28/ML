For a given csv file homeprices.csv which contains feilds area, bedrooms, age and price apply linear regression model on this data and predict the prices based on area, bedrooms and age. Also before appling the model clean the data by filling the missing block in the bedrooms coloumn. To fill the missing block find the median of the bedroom col values and fill it in missing places using fillna.
